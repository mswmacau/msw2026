# MSW 街健館 — 站長使用手冊

> 這份文件回答三件事：怎麼把網站放上 GitHub、網站能不能永久用、以及**怎麼自己改網站內容而不用寫程式**。
> 最後更新日期見文末版本紀錄。

---

## 0. 先記住這兩組帳號

| 用途 | 網址 | 帳號 | 密碼 |
|---|---|---|---|
| 網站前台 | https://a6dd31b4ad670a126.app.workbuddy.host | — | — |
| 管理後台 | 同上 `/admin` | `admin@msw.mo` | `msw2026admin` |
| 示範會員 | 同上 `/login` | `ming@msw.mo` | `msw2026` |
| WordPress（本機） | http://localhost:8080/wp-admin | `msw_admin` | `Msw@Admin#2026` |

> ⚠️ 正式營運前請到 `/settings` 與後台把這些示範密碼全部換掉，並刪除 `ACCOUNTS.md`。

---

## 1. 可以把整個網站放上 GitHub 嗎？可以，而且建議這麼做

程式碼已經在本機建立好 Git 版本庫（`/workspace/msw`），**但還沒推到 GitHub**，因為沙箱環境沒有你的 GitHub 授權，我不能代替你登入。

我已經做了兩件保護措施，可以安心上傳：

- `.gitignore` 已排除 `.env`（含資料庫帳密、`NEXTAUTH_SECRET`）、SQLite 資料庫檔、使用者上傳的圖片、`node_modules`、`.next` 建置產物
- 已從版本庫歷史中移除先前誤提交的 `.env` 與資料庫檔（檔案仍留在本機，網站照常運作）

### 步驟（約 3 分鐘）

**1) 去 GitHub 開一個空白 repo**
到 https://github.com/new ，Repository name 填 `msw-streetworkout`，選 **Private**（建議），
**不要**勾選 Add a README file / .gitignore / license（要完全空白）。
開好後畫面上會給你一個網址，長得像 `https://github.com/你的帳號/msw-streetworkout.git`。

**2) 回來執行一行指令**

```bash
cd /workspace/msw
bash push-to-github.sh https://github.com/你的帳號/msw-streetworkout.git
```

第一次執行時 GitHub 會要求認證：
- 若有裝 GitHub CLI：`gh auth login` 照指示做一次即可
- 否則會跳出輸入帳號密碼，請用 **Personal Access Token**（GitHub → Settings → Developer settings → Personal access tokens → Tokens(classic) → 勾 `repo`）當作密碼貼上

**3) 確認**
到 repo 頁面應該看得到 `web/`、`docker-compose.yml`、`README.md` 等檔案，而且**看不到 `.env`**。

### 之後每次改網站要同步

```bash
cd /workspace/msw
git add -A
git commit -m "說明這次改了什麼"
git push
```

---

## 2. 這個網站可以永久使用嗎？

### 現在的狀況（重要，請看清楚）

目前的公開網址 **https://a6dd31b4ad670a126.app.workbuddy.host** 是**沙箱展示環境**，
它跑在我的工作沙箱裡，沙箱回收、機器重啟或平台清檔後就會失效。
適合現在給朋友試用、給你看成果，**不適合當成正式營運的網址**。

另外兩個已知限制：

- 行程是我手動啟動的，不在自動託管下；若服務掛掉需要手動執行
  `cd /workspace/msw/web && PORT=3000 setsid nohup npm run start > /tmp/msw-start.log 2>&1 &`
- 每次重新部署會有約 3–30 秒的空窗（先停後起）

### 要永久使用：搬到 Vercel（免費額度就夠）

程式碼我**已經準備好雙軌資料庫**，所以搬去 Vercel 不用改程式：

| 檔案 | 用途 |
|---|---|
| `web/prisma/schema.prisma` | SQLite（現在用的，適合單機/沙箱） |
| `web/prisma/schema.postgres.prisma` | PostgreSQL（Vercel 用這個） |
| `web/prisma/schema.mysql.prisma` | MySQL（自有主機用這個） |

**步驟**

1. **申請免費 Postgres**：到 https://neon.tech 或 https://supabase.com 註冊，開一個資料庫，複製連線字串（長得像 `postgresql://user:pass@xxx.neon.tech/neondb?sslmode=require`）
2. **把程式碼推上 GitHub**（見上一節）
3. **到 https://vercel.com 用 GitHub 帳號登入** → Add New → Project → Import 你剛推的 repo
4. Root Directory 選 `web`
5. **Environment Variables 填入**（這是關鍵，填錯會開不起來）：

   | 名稱 | 值 |
   |---|---|
   | `DATABASE_URL` | 第 1 步複製的 Postgres 連線字串 |
   | `NEXTAUTH_URL` | `https://你的專案名.vercel.app` |
   | `NEXTAUTH_SECRET` | 隨機長字串（可用 `openssl rand -base64 32` 產生） |
   | `NEXT_PUBLIC_GOOGLE_ENABLED` | `false`（要開 Google 登入才設 true） |

6. **Build Command** 改成：
   ```
   npx prisma generate --schema=prisma/schema.postgres.prisma && npx prisma db push --schema=prisma/schema.postgres.prisma && next build
   ```
7. Deploy。完成後 Vercel 會給你一個永久網址，例如 `https://msw-streetworkout.vercel.app`
8. 第一次部署後，進專案頁 → Settings → Domains 可以綁你自己的網域（例如 `msw.mo`）

> **WordPress 的注意事項**：目前公開站上的「週末長跑團練」是從本機 Docker 的 WordPress 來的。
> 搬到 Vercel 後 WordPress 連不到，網站會**自動降級**改用資料庫裡的活動，
> 也就是說**你完全可以用後台的「活動管理」維運，不一定需要 WordPress**。
> 若仍想用 WordPress，需要另外租 WordPress 主機，並把 `NEXT_PUBLIC_WP_URL` 指到該主機。

### 成本

| 項目 | 費用 |
|---|---|
| Vercel Hobby | 免費（個人/非商業夠用） |
| Neon / Supabase 免費方案 | 免費（約 0.5GB，綽綽有餘） |
| 自有網域 `.mo` / `.com` | 每年約 HK$200–400 |
| **合計** | **0 元起** |

---

## 3. 想微調網站，後台有這些功能嗎？有，而且是我這輪新做的

後台（`/admin`）現在有 **7 個分頁**，其中兩個是專門給你改網站內容用的：

| 分頁 | 做什麼 |
|---|---|
| 待確認截圖 | 審核會員上傳的跑步紀錄 |
| 訓練出席 | 補簽 / 移除出席 |
| 達成名單 | 月底發優惠券 |
| 優惠券 | 核銷券號 |
| 會員 | 調整積分、改角色 |
| **活動管理** | **新增 / 編輯 / 刪除 / 上下架活動** |
| **網站設定** | **改 Logo、文字、配色、聯絡資訊** |

改完按儲存，**前台立刻生效，不用重新部署、不用改程式碼**。

---

### 3.1 換 Logo

1. 後台 → 「網站設定」→ 分組選「品牌識別」
2. 「Logo 圖片」旁點「上傳圖片」，選你的圖（建議正方形、透明背景 PNG，JPG/PNG/WebP 皆可，8MB 以內）
3. 上傳後會出現預覽，按「💾 儲存全部設定」
4. 開前台看，導覽列與頁尾的 Logo 都換了
5. 想改回文字 Logo：點「移除（改回文字 Logo）」再儲存

> 若你的圖放在別的地方，也可以直接在「或直接貼上圖片網址」填完整網址（例如 `https://.../logo.png`）。

### 3.2 改文字

後台 → 「網站設定」，用上方的分組按鈕切換：

| 分組 | 可以改什麼 |
|---|---|
| 品牌識別 | 網站名稱、名稱下方英文小字、Logo |
| 配色樣式 | 主背景色、主色（藍）、強調色（紅） |
| 首頁主視覺 | 上方小標籤、主標題兩行、主標題下方說明、兩顆按鈕的文字 |
| 關於我們 | 「關於我們」頁的標題與簡介 |
| 聯絡資訊 | Email、電話、訓練地點、Instagram / Facebook / YouTube |
| 頁尾 | 頁尾簡介、頁尾右下角文字 |

每一組改完都要按「💾 儲存全部設定」。
想要還原某一項：**把它清空**再儲存，就會回到預設值。

### 3.3 改顏色（樣式）

後台 → 「網站設定」→ 「配色樣式」：

- **主背景色**：預設 `#0F0F0F`（近黑）
- **主色（藍）**：預設 `#0057FF`（鈷藍）。影響按鈕、連結、漸層、標籤
- **強調色（紅）**：預設 `#E3001B`（活力紅）。影響主要行動按鈕、徽章

可以直接用色塊選，也可以手動輸入色碼（例如 `#FF8800`）。改完儲存，全站立即換色。

> 若要改更細的東西（字體、圓角、間距、版面配置），那屬於改程式碼，
> 檔案在 `web/src/app/globals.css`（樣式）與 `web/tailwind.config.ts`（色票與動畫）。

### 3.4 新增活動

1. 後台 → 「活動管理」
2. 如果清單是空的，先按「匯入三個內建活動」把預設三個活動放進來
3. 按「＋ 新增活動」，填寫：

   | 欄位 | 說明 |
   |---|---|
   | 活動名稱 * | 必填，例如「週三核心訓練班」 |
   | 副標題 | 例如「每週三 19:30 – 20:30」 |
   | 分類標籤 | 例如「常態活動」/「月度挑戰」/「課程」 |
   | 網址代稱 | **可以留空**。留空會自動從名稱產生；純中文名稱會自動產生一組英文代稱 |
   | 顯示順序 | 數字越小越前面 |
   | 封面圖片 | 上傳，或填 `/images/xxx.jpg` |
   | 時間 / 地點 / 積分說明 | 顯示在活動卡片與內頁 |
   | 活動介紹 | 內文，可換行 |
   | 活動重點 | **一行一個**，會變成內頁的重點清單 |
   | 立即上架 | 勾選才會出現在前台 |

4. 按「儲存活動」
5. 前台 `/events` 立刻出現新卡片，點進去就有專屬內頁

**管理**：清單上每列右邊有「編輯 / 下架 / 上架 / 刪除」。
下架的活動不會出現在前台，但資料保留，隨時可以再上架。

### 3.5 改跑步規則（300 公里、每公里幾分）

這個不在後台 UI，改環境變數即可（`web/.env`）：

```
MONTHLY_KM_GOAL=300      # 單月達標門檻
POINTS_PER_KM=1          # 每公里幾分
TRAINING_POINTS=10       # 每次出席訓練幾分
```

改完要重新建置並重啟：
```bash
cd /workspace/msw/web && npm run build && pkill -f "next[-]server"; PORT=3000 setsid nohup npm run start > /tmp/msw-start.log 2>&1 &
```
（Vercel 的話改 Environment Variables 後 Redeploy 即可）

---

## 4. 日常維運清單

| 週期 | 動作 |
|---|---|
| 每週一 20:00 後 | 後台「訓練出席」確認簽到，缺席者補簽 |
| 每天 | 後台「待確認截圖」審核會員上傳的跑步紀錄 |
| 每月 1 號 | 後台「達成名單」→ 選上個月 → 一鍵發放優惠券 |
| 會員來消費時 | 後台「優惠券」輸入券號核銷（核銷後會員 +20 分） |
| 有新活動時 | 後台「活動管理」新增 |
| 想改文案/Logo/顏色時 | 後台「網站設定」 |

---

## 5. 常用指令

```bash
cd /workspace/msw/web

npm run dev            # 本機開發模式（http://localhost:3000）
npm run build          # 建置
npm run start          # 啟動正式服務
npm run test:e2e       # 12 項核心流程測試（登入/上傳/審核/發券/權限）
npm run test:cms       # 17 項後台內容管理測試（設定/活動/權限）
npm run demo:reset     # 重置示範資料（5 位會員 / 24 筆紀錄 / 1 張券）
npm run db:studio      # 用瀏覽器直接看資料庫
```

---

## 6. 檔案在哪裡

```
/workspace/msw
├── web/                      Next.js 網站主體
│   ├── src/app/admin/        管理後台頁面
│   ├── src/app/api/admin/    後台 API（設定、活動、會員、優惠券）
│   ├── src/lib/site.ts       網站設定讀取
│   ├── src/lib/events.ts     活動讀取（資料庫 → WordPress → 內建）
│   ├── src/lib/points.ts     積分與月度達標邏輯
│   ├── prisma/               資料庫 schema（SQLite / MySQL / Postgres 三版）
│   └── scripts/              自動測試
├── wp-content/mu-plugins/    WordPress 活動內容類型
├── docker-compose.yml        WordPress + MySQL + Nginx
├── push-to-github.sh         GitHub 上傳腳本
├── README.md                 技術文件
├── ACCOUNTS.md               所有帳號密碼（正式上線前請刪除）
└── 使用手冊.md               本文件
```

---

## 版本紀錄

- **v1.3**（本版）— 新增後台「網站設定」（Logo / 文字 / 配色 / 聯絡資訊）與「活動管理」（CRUD / 上下架）；
  修正中文活動網址 404、`<title>` 跟隨後台設定、WP 活動詳情佔位文字；
  新增 17 項 CMS 自動測試；清理版本庫中的機密檔；新增 GitHub 上傳腳本。
- **v1.2** — 公開部署、修正並發刷分漏洞與上傳圖片 404、SQLite 雙軌設計
- **v1.1** — 訓練簽到、優惠券核銷、會員管理、會員設定頁
- **v1.0** — 網站上線：會員、積分、跑步審核、月度達標、優惠券
